# HANDOFF 六爻引擎 0816

接上一棒 `HANDOFF_六爻引擎_0815c.md`(工作線:side-projects/destiny 六爻引擎)

---

## 1. 現況一句話

執行完上一棒的下一步(「用神有根」結構化成欄位),再做了一輪**古籍原文校訂**把第一批
命中率從 75.5% 推到 **79.2%**;**四個改動全部在工作區,一個 commit 都沒有**。

---

## 2. 已完成

### (1) `yongShenGen` 欄位化 —— 上一棒指定的下一步

`LiuyaoReport.yongShenGen`(`hasRoot` + 9 個子項)。判定式原本散在旬空段與原神段的
區域變數裡,算完即棄,外面拿不到。

- **不參與計分,且是驗過的**:64 卦 × 6 動爻 × 21 類別 × 4 組日月 × 2 種 intent
  = **64,512 筆完整輸出(含每段斷語文字)逐字比對 Δ=0**;校準三數字一字未動
- `engine.test.ts` 加 7 則,守的是**欄位與斷語不得分岔**(而非判得準不準),
  含「九個布林在卦象空間裡都要兩種值都出現」防退化成常數
- **沒有任何消費者**:不進 sections、不碰 score、UI 不顯示。刻意的

### (2) 四項原文校訂 —— 第一批 75.5% → **79.2%**(領先基準 15.1 → **18.9pp**)

**沒有調任何權重。** 做法:把第一批 13 則失分逐則攤開看成因,分「資料/對應問題」
「古法未實作」「引擎內部不一致」三類,只動後兩類,每條回殆知閣抓 `增删卜易.txt` 核原文。

| # | 修正 | 依據 | 第一批 Δ | 窮舉觸發率 |
|---|---|---|---|---|
| 1 | 進退神補上土支(`data/core.ts`) | 《進神退神章第二十九》原文列舉 | 0 | **8.18%** |
| 2 | 月破分真破/目下破 | 《月破章第二十七》野鶴修正 | 0 | **2.80%** |
| 3 | 占病六沖列為主導條件 | 占病門逐條 + 〈六沖章第二十〉 | **+2**(#10、#48) | 2.98% |
| 4 | 伏神為用神時的六親判定(bug) | — | 0 | — |

1. `core.ts` 原註解「古法僅論四組,辰戌丑未不在其列」**是錯的**。原文 7 進 7 退,
   土支在列;失分的 #8、#10 用的正是上爻**戌化未**。土是 丑→辰→未→戌 線性序,
   原文沒有戌化丑,測試釘住不准補成循環。
2. 引擎一律 −3,實作的正是野鶴指名要捨棄的那套(「諸書皆以用神臨月破…即是枯根木」),
   **與「用神多現」當初的錯法同型**。改成真破(靜+無日辰生助+無動爻生助)−3、
   目下破 −0.6。`−0.6` 是**類比**旬空段「遲而未顯」取的,**沒掃過命中率**。
3. 卦格段早就把「古云『久病逢沖莫治』」印在報告上,**卻沒接進 decisive**,說了不算數。
   只推 decisive 不計分(沖合分數由卦格段獨佔,有測試釘住占病段不得再敘述一次)。
4. 占病段兩處寫 `target.liuqin`,但用神為伏神時用神是 `target.fuShen`;
   #48 因此被斷成「子孫持世而子孫即用神本身」——用神明明是伏著的申金兄弟。

### (3) 修正 (1) 自己寫錯的 `hasRoot` 定義

初版把 `dongFu`(動爻生扶)算進有根。《元神忌神衰旺章》原文正好相反:
「其如亥水月沖日剋…**雖有生扶生之不起,如樹無根**…此謂之用神無根,元神有力亦難生」。
整句話的重點就是別人來生補不了自己沒氣。已改為
`hasRoot = !真空 && !真破 && (有氣 || 日扶 || 月扶)`,並加測試釘住「dongFu 不得單獨撐起 hasRoot」。

### 驗證證據

```
npm test                        131 項全過(session 開始時 116)
npx tsc -b / oxlint src / build 皆乾淨
第一批  n=53 hit=79.2% base=60.4% lead=18.9pp   (開始時 75.5% / 15.1pp)
第一批主導條件  觸發 11 命中 10 (91%)，新增四項六沖 5/5
窮舉 64,512 筆  方向翻轉 1.52%、判語改變 1.94%、任一欄差異 18.94%
```

**UI 未重跑**——這幾次改動都不觸及 UI 路徑。上一次 playwright 實跑是時點推演那版。

---

## 3. 未完成與地雷

🔴 **本 session 的產出全部在工作區,零 commit。** 5 個檔案:
`CLAUDE.md`、`README.md`、`src/lib/data/core.ts`、`src/lib/engine.test.ts`、`src/lib/interpret.ts`
(+571 −41)。HEAD 仍是 `54a4a7b`,與 origin/main 同步,未推 commits 0。
**問過一次要不要 commit,william 當時的回覆是「先繼續增加準確率」,等於沒答。**

🔴 **保留集我沒有跑,而且是刻意的。** 第三批採不出來、保留集看一次少一次,
而這輪改動全部由原文與內部一致性支撐,不需要它裁決。
- README/CLAUDE.md 的保留集與整體數字**已撤下標「未量測」**,沒有留舊值假裝現況
- 唯一已知的下界:`calibration.test.ts` 那道「保留集領先 ≥8pp」的**既有**常設防線仍過關
  (是它順帶告訴我的,不是我去看的)
- **要不要花掉這一次驗收是 william 的決定**;跑完要把 CLAUDE.md:48 與 README:75 兩張表補齊

🔴 **CI 仍紅、仍從未跑過測試。** 與上一棒相同,與本棒改動無關:Pages 至今未啟用。
131 項綠燈全是本地。上一棒查出失敗訊息是 workflow 內建 token 權限不足,
`gh api -X POST repos/williamlabdev/plum-blossom-divination/pages -f build_type=workflow`
可能一行就開——**至今仍未驗證**。

⚠️ **#5 的兩條修正剛好抵銷,不要為了救它去偏袒其中一條。** 修正 2 把它 −0.90 → +1.50,
修正 1 又因「戌化未為退神」扣回 −1.5,淨值 0.00 落在平,仍是失分。
野鶴自己也注意到這個張力(「古訣…謂之動逢月破…為退之不及。此卦父爻破而動而化空,
竟退以歸家」)。兩條各有原文撐腰。

⚠️ **看過但刻意不做的三件事**(已寫進 CLAUDE.md「還沒做、但看過的三件事」):
- **#50**:主導條件唯一失手,原文自己給了推翻條件「但不宜世持忌神剋之,此病必危」。
  不實作——它會是「看著訓練集一則失分加進來的古籍明文規則」,形狀跟「忌神持世」一樣
- **#22/#23**:兩則都是兄弟持世,正是「忌神持世」當初的誘因。不碰
- **#5/#36**:問時機型,古人給的是應期不是吉凶。維持不移出分母

⚠️ 保留集仍是唯一獨立測試集,已被消耗三四次,**沒有備援**。

⚠️ `−0.6`(月破目下破)與 `DECISIVE` 新增的四項六沖,**都還沒在任何獨立語料上驗過**。

---

## 4. 下一步

**(1) 先問 william 兩件事,不要自己決定:**

- a. 這 5 個檔案要不要入庫?要的話:
  ```bash
  cd /Users/william/dev/source/side-projects/destiny/plum-lossom-divination
  git add -A && git commit -m "fix: 四項古籍原文校訂——進退神土支、月破真破之分、占病六沖主導、伏神六親"
  git push
  ```
- b. 要不要花掉保留集這次驗收?要的話跑完**務必**把 CLAUDE.md:48 與 README:75 補齊:
  ```bash
  # 臨時檔跑完請刪
  npx vitest run <臨時檔> --disable-console-intercept   # runAllCases + summarise(slice(HOLDOUT_START))
  ```

**(2) 若要繼續推準確率**,剩下 11 則失分裡沒有「靠原文就能修」的了。
下一個可辯護的方向只有**語料來源**,不是調參——見 CLAUDE.md「第三批採集的結果」。

**(3) 修 CI**——william 手動,或試上面那行未驗證的 `gh api`。

**(4) backlog #4 多爻作用結算層** —— 仍卡在 §5 的產品決策,未動。

---

## 5. 勿碰/等待

**等 william 裁決:**

1. **本棒 5 個工作區檔案的去留**(見 §4-1a)。要整包退就 `git checkout -- .`
2. **要不要跑保留集驗收**(見 §4-1b)——這是「看一次少一次」的資源
3. **要不要加六爻搖錢法輸入(backlog #4 的真正閘門)** —— 承襲前兩棒,**未變,仍等裁決**

**承襲未閉合:** #69 的複驗條件仍無解(第三批採不到,前提已變,見 0815c §3);
此 repo 未納管(`portfolio-status.md:152`)。
