# HANDOFF — 六爻引擎(2026-08-15 第三棒 / 0815c)

接續 `HANDOFF_六爻引擎_0815b.md`。本棒只做一件事:**執行上一棒的「下一步(2) 採集第三批語料」**。

---

## 1. 現況一句話

第三批語料**採不出來**——可及來源已掃盡,48 則候選裡只有 15 則可收,樣本量不足以當保留集;
這條路已封,結論與工具都寫進 repo,`CLAUDE.md` 那句「若要再做大幅調參就採第三批」已改寫。

---

## 2. 已完成

### (1) 易藏全掃:確認沒有新書(這是本棒最主要的產出)

掃過殆知閣 **易藏全部 342 檔**(术数 155 + 易经 188,99MB),
**命中的書就是已知那 6 本,沒有任何新書貢獻一則**。

- 未挖候選 **卜筮元龜、斷易鬼靈經、卜法詳考、周易筮述、斷易天機** → 全部貧瘠。
  周易筮述有卦例但屬《周易》古筮法(「遇小畜二爻」),無月建日辰,對本引擎無用。
- **黃金策(易藏獨立本)** 6 則與卜筮全書內收的黃金策同源,全部重複。
- 全庫其餘 15352 檔沒掃:檔名關鍵字過濾後只有 27 檔沾邊,且全是佛經(占察善惡業報經)、
  武術(八卦拳學)、職官(筮仕金鑑)之類。**不值得為此 clone 2.1GB**
  ——william 當場擋下了我的 clone 提議,改用已下載的 file tree 篩選,範圍差 20 倍。

### (2) 三個踩到的坑,已寫進 CLAUDE.md

前兩個會**整批**漏案例(CLAUDE.md 原本只記了卜筮全書的數字月份):

1. **易冒寫「X建」不寫「X月」**(「午建甲戌日」)——只認「月」會漏掉它 56 個錨點中的 54 個。
2. **增刪卜易有一批用「變」不用「之」且用卦全名**——「得乾為天變天小畜」「得既濟卦變革卦」。
3. **月日錨點離卦例太遠會抓到前一則案例的日期**——放寬到 100 字時實測 **4/24 則**的
   月建日干支是錯的(例:標成「酉月丙辰日」,原文其實是「庚寅年己卯月癸卯日,父卜子回,得艮之蠱」)。
   收緊到 ≤35 字且不跨句號才乾淨,代價是候選從 56 降到 45。

### (3) 15 則已收案例(未併入語料庫)

`src/lib/data/classic-cases-batch3.json`,**沒有併入 `classic-cases.json`、不參與計分、
`calibration.ts` 沒有 import 它**(`grep -rn 'classic-cases' src/lib/*.ts` 只有 calibration.ts:29 那一行)。

漏斗:

| 階段 | 則數 |
|---|---|
| 嚴格候選(月日錨點 + 單動爻 + 有應驗詞 + 未收) | 48 |
| 天時類,依收錄標準排除 | 9 |
| 夾註中的法則示範(「如午建甲戌日,用官得革之同人」),無真實占事 | 5 |
| 斷完即「命之再占」或只有斷語、無應驗結果 | 16 |
| 其餘不合格(非吉凶題等) | 3 |
| **實際可收** | **15** |

複驗:XOR 動爻 / 六十甲子 / 卦名合法 / 月建,**15 則全數通過**。
**但 `source` 的章節是程式從原文回溯的,可能差一章**(有兩則距最近章名太遠,只標了書名)。

### (4) 採集工具

`tools/collect-cases.py` — 自足腳本,含取原文的 curl 指令與四個坑的程式對應;
卦表直接吃 `src/lib/data/zhouyi.ts` 不另維護。跑完輸出 `tools/candidates.json`(88K,含 ctx 供人判讀)。

```bash
python3 tools/collect-cases.py <books目錄>
# 掃 12 檔 → 命中 129 → 未收 48
```

### (5) 文件

- `CLAUDE.md`「案例從哪裡來」:加了貧瘠書目、**「這條路已經走到底了」**、兩個格式陷阱、
  「第三批採集的結果:採不出來」表。
- `CLAUDE.md`「訓練/測試分割」:原本寫「若要再做大幅調參,正確做法是重新採集第三批當測試集」
  ——**已改寫**,因為實際去採了採不出來,改成「保留集短期內無法被替換,
  再做大幅調參就是在消耗僅有的獨立性,要有這個自覺」。

### 驗證證據

```
npm test  →  Test Files 4 passed (4)   Tests 116 passed (116)     ← 本地
```

**本棒未動任何計分邏輯、未動 `classic-cases.json`、未動權重常數**,校準數字結構上不可能變。

---

## 3. 未完成與地雷

🔴 **CI 仍紅,仍從未跑過測試。** 與上一棒完全相同,且**與本棒改動無關**:
`gh api repos/williamlabdev/plum-blossom-divination/pages` 回 404,Pages 至今未啟用。
最新失敗 run `31889495837`,9 秒掛在 `actions/configure-pages@v5`,
錯誤是 `Resource not accessible by integration`。**上面 116 項綠燈是本地跑的。**

> 本棒發現的一件事:那個錯誤是 **workflow 內建 `GITHUB_TOKEN` 權限不足**,
> 不是「Pages 無法自動化」。william 本機 `gh` token 有 `repo` scope,可能一行就能開:
> `gh api -X POST repos/williamlabdev/plum-blossom-divination/pages -f build_type=workflow`
> ——**沒有跑過,未驗證**;william 當時選擇先不管 CI 去做採集。

⚠️ **15 則不足以當第三批保留集**,理由是量測能力不是品質:
吉 9 / 凶 4 / 平 2,全猜吉基準 60%,binomial 雜訊 ±25pp;
要驗的 #69 那類是 1 則 ≈ 6.7pp 的差異,**沒有鑑別力**。
低信心比例 7/15(47%),遠高於現有語料的 20/101(20%)。
**不要因為「有 15 則了」就把它併進 `classic-cases.json`。**

⚠️ **#69 的複驗條件現在無法滿足。** 上一棒裁定 #69 留著,附的複驗條件是
「第三批採集後若這類案例仍成群失分就回頭 revert `c836acc`」——**第三批採不到,這個條件短期內無解**。
這不是重開裁決,是記錄前提已變。

⚠️ 保留集仍是唯一的獨立測試集,且已被消耗三四次。再做大幅調參就是在消耗它,沒有備援。

---

## 4. 下一步

**(1) 唯一不依賴新語料的推進方向:把「用神有根」結構化成欄位**(= 上一棒的下一步(3))

```
src/lib/interpret.ts   旬空段(真空判定)、原神段(生扶判定)
```
兩處已各自算出「用神是否有根」,只是沒留成 `LiuyaoReport` 的欄位。
這是古法允許斷「遲而後成」的前提,也是時點推演接回計分唯一可辯護的入口。
**先做這個前置,不要直接調條件。**

**(2) 本棒的產出待 william 決定去留**(三個檔都還沒 commit,見 §5)

**(3) 若要修 CI**——william 手動,或試上面那行未驗證的 `gh api`:

```bash
gh api -X POST repos/williamlabdev/plum-blossom-divination/pages -f build_type=workflow
gh run rerun 31889495837 && gh run watch
```

**(4) backlog #4 多爻作用結算層** —— 仍卡在 §5 的產品決策,未動。

---

## 5. 勿碰/等待

**等 william 裁決:**

1. **本棒三個檔要不要留**(已 commit 到本地,見下方 hash;要退就 revert 那一個 commit):
   - `CLAUDE.md` 的改寫(尤其是把「採第三批」那句改掉——那是上一棒寫的話)
   - `src/lib/data/classic-cases-batch3.json` 15 則
   - `tools/collect-cases.py` + `tools/candidates.json`
2. **要不要加六爻搖錢法輸入(backlog #4 的真正閘門)** —— 承襲上一棒,**未變,仍等裁決**。
   產品範圍決策,不拍板就不要動多爻結算層。

**勿碰:**

- 不要把 15 則併進 `classic-cases.json`(理由見 §3)。
- 不要把問時機型案例移出校準分母(已裁決不做)。
- 時點推演不要在沒有新語料下「調一調就接回去」——而現在已知**不會有新語料**。
