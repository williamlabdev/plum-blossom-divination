# HANDOFF — 六爻引擎 0816b（承 `HANDOFF_六爻引擎_0816.md`）

> 上一棒:`HANDOFF_六爻引擎_0816.md`(09:31)。本檔寫於 0816 稍晚。
> 工作線:side-projects/destiny 六爻引擎。

---

## 1. 現況一句話

上一棒卡著的兩件裁決都做掉了(四項校訂已入庫、保留集決定先不跑),CI 從「從未跑過測試」
修到真的綠、網站上線、發了 v0.1.0;之後再做一輪原文校訂,**第一批命中率沒有動(仍 79.2%)**,
但找到兩處引擎與原文不符並修正。工作區乾淨,未推 0,無開著的 PR。

---

## 2. 已完成

### (1) 上一棒兩件待裁決 —— william 當場裁決並執行

- **5 個工作區檔案入庫**:`59fc778`(+571 −41),已 push。裁決是「commit 並 push」。
- **保留集**:裁決是「**先不跑**」。這次驗收仍留著。CLAUDE.md:48 與 README:75
  兩張表維持「未量測」,沒有留舊值假裝現況。

### (2) CI 修好 —— 從「從未執行過測試」到綠

**根因不直覺,值得記著**:失敗**不是** `permissions:` 給錯,而是 GitHub Actions 的內建
`GITHUB_TOKEN` **結構上無權建立 Pages site**。`deploy.yml:22-24` 的 `enablement: true`
在 site 不存在時永遠打不過去(`Resource not accessible by integration`),所以每次 run
都 11 秒暴斃在 `configure-pages`。site 必須先由外部憑證建立一次:

```bash
gh api -X POST repos/williamlabdev/plum-blossom-divination/pages -f build_type=workflow
```

**這行會被 Claude Code 的外向分類器擋(與 `gh pr merge` 同類),必須 william 自己跑。**
`gh` 的 `repo` scope 就夠,不必另建 PAT,也不要為此把 PAT 長期放進 repo secrets。

證據:
- run `31920029235`(手動觸發):4 檔 **131 項全綠**、build、deploy 皆成功
- run `31920097135`(push 觸發):success —— 確認 push 路徑通了,不是一次性僥倖
- 網站 `https://williamlabdev.github.io/plum-blossom-divination/` **HTTP 200**
- 文件已同步:`4732886`(CLAUDE.md backlog #6 標完成並記根因、README 部署段)

**在此之前 CI 從未執行過任何測試**——先前所有「131 項全過」都只是本地證據。

### (3) 首次發版 v0.1.0

- 版號 `0.0.0` → `0.1.0`(`0961c4e`,package.json + package-lock.json)
- annotated tag `v0.1.0` → `0961c4e`,已推
- Release: https://github.com/williamlabdev/plum-blossom-divination/releases/tag/v0.1.0
  (正式發佈,非 draft、非 prerelease)
- 版號與 release notes 寫法都由 william 選:0.1.0、且 notes 要「寫第一批＋明講保留集未量測」
- notes 內含 ±4.8pp 雜訊、只驗方向、驗案集先驗偏差、兩條規則曾被保留集拆穿為過擬合,
  以及三條已知限制。**沒有把未量測的數字寫成數字。**

### (4) 兩項原文校訂 —— **第一批 Δ=0**(`7b03e5f`)

起點是**不採信上一棒「剩下 11 則沒有靠原文就能修的了」那句自評**(同一 session 對自己
工作的判斷)。重新攤開 11 則分類後,找到兩處引擎與原文不符:

| # | 修正 | 依據 | 第一批 Δ | 窮舉影響 |
|---|---|---|---|---|
| 5 | 暗動／日破限靜爻,動爻逢日沖改依動散章 | 〈暗動章第二十二〉〈動散章第二十三〉 | 0 | 觸發 1.25% |
| 6 | 真空收緊為「月令死」,休囚而空改判假空 | 〈旬空章第二十六〉 | 0 | 真空 4.07% → 1.26% |

合計:輸出差異 5.32%、判語改變 1.13%、**吉凶方向翻轉 0.20%**(32,256 筆窮舉)。
**Δ=0 但不是死程式碼**——與上一輪三項 Δ=0 的校訂同型。細節與原文引用全在 CLAUDE.md
「兩項原文校訂(2026-08-16 稍晚)」一節,此處不重複。

順帶抓到三件事(都已寫進 CLAUDE.md):
- **旬空段從來沒有用過 `zhenKong` 欄位,自己另算一套。**第一次只改欄位時窮舉 0 筆差異
  才發現。`engine.test.ts` 那組「欄位與斷語不得分岔」測試**當場擋下來了**——它有在做事。
- 舊條件裡的 `!yYouQi` 與 `!monthFu` 邏輯等價,是冗餘。
- **#40 上一棒分類錯了**:原文「卦中陽動變陰,必然主女」斷的是男女不是吉凶,
  古人記「吉」來自「生女、產母平安」。它與 #5／#36 同型,屬對應問題,不是「古法未實作」。

### 驗證證據

```
npx vitest run          → 4 檔 132 項全過(131 → 132,新增真空邊界的一對測試)
npx tsc -b --force      → 乾淨
npx oxlint src          → 乾淨
npm run build           → 乾淨
CI run 31924776048      → success(7b03e5f)
git status --porcelain  → 0
git log --branches --not --remotes → 0
```

窮舉腳本與診斷腳本都是臨時檔,**已刪除**(`src/lib/_tmp-misses.test.ts`、`_tmp-sweep.test.ts`)。

---

## 3. 未完成與地雷

⚠️ **準確率這一輪沒有推動。第一批仍 79.2%、仍是同樣 11 則。**
不要因為 commit 訊息寫「校訂」就以為數字進步了。兩項改動的價值在正確性。

⚠️ **保留集仍未量測,且刻意如此**(william 0816 裁決「先不跑」)。
它已被消耗三四次、第三批採不出來、**沒有備援**。CLAUDE.md:48 與 README:75 兩張表
在跑之前不要填。已知下界只有常設防線「保留集領先 ≥8pp」仍過關。

⚠️ **新加的兩個權重(動爻逢日沖休囚 −0.6、休囚而空 −0.6)都沒有在任何獨立語料上驗過。**
兩者都是照既有同型斷語(月破「目下破」、旬空「有生扶不為空」)類比取的,
**刻意沒有掃描命中率**——掃了就成了用語料調參。動它的人請保留這個性質。

⚠️ **`eachChart` 的第五組日月(`['寅','甲','子',['戌','亥']]`)是為了讓真空取樣得到才加的,
別拿掉。** 真空收緊後前四組**沒有一組**能產生真空,那三則測試會退化成空轉。

⚠️ 承襲未變:#5 兩條原文剛好抵銷(不要偏袒其中一條);#22／#23 兄弟持世(不碰);
#50 主導條件唯一失手、原文自己給了推翻條件(不實作,形狀與「忌神持世」相同)。

⚠️ Node 20 deprecation 警告:`deploy.yml` 用的 4 個 action 都還 target Node 20,
GitHub 已強制在 Node 24 上跑。目前只是 annotation,不影響 CI 結果,但遲早要升。

---

## 4. 下一步

**唯一還有原文撐腰、刻意沒動的線索 ——「辰戌丑未月算土令,還是併入前一季?」**

真空那句訣「春土、夏金、秋木、三冬逢火」的四時是木火金水各主一季(春＝寅卯辰),
土沒有自己的令;本引擎 `najia.ts` 的 `wangShuaiOf` 以月支自身五行為令。
兩套在土月的「死」是哪一行上直接衝突(原文未月屬夏、死者為金;本引擎未月屬土、死者為水)。
**校準 #8 恰好卡在這個分歧上**(未月占功名、用神亥水旬空:本引擎判死→真空→−2;
原文那套判囚→根本不入真空)。

要動的話當成一次**獨立的架構決策**,順序:

1. 先確認 `wangShuaiOf` 的全部消費者能不能接受(月建段、原神有力、入墓、`genFactor`、
   時點推演的逐月旺衰):`grep -rn "wangShuaiOf\|wangShuai" src/lib/`
2. 改之前先錄行為快照(窮舉 32,256 筆),改後比對方向翻轉率
3. **不要為了救 #8 順手改**——那就是「看著訓練集裡一則失分改結構」,形狀與「忌神持世」一樣

**其餘方向的現況(不要重新評估一次,已有結論):**
- 原文這口井這一輪又挖出兩桶,但**明顯見底**。語料才是瓶頸,不是規則。
- 第三批語料已確認採不出來(0815 掃盡易藏 342 檔)。要再調參得先解決語料來源。
- backlog #4 多爻同動:閘門是「要不要加六爻搖錢法輸入」這個產品決策,承襲五棒未變。

**若只是要驗收現況:**
```bash
cd /Users/william/dev/source/side-projects/destiny/plum-lossom-divination
npm test                                          # 132 項
open https://williamlabdev.github.io/plum-blossom-divination/
```

---

## 5. 勿碰/等待

**等 william 裁決:**

1. **辰戌丑未月的四時歸屬**(見 §4)——牽動全盤旺衰,不要順手做。
2. **要不要花掉保留集這次驗收**——0816 已裁決「先不跑」,除非他改變主意。
   跑完務必補 CLAUDE.md:48 與 README:75 兩張表。
3. **要不要加六爻搖錢法輸入**(backlog #4 的真正閘門)——承襲五棒,**未變,仍等裁決**。

**承襲未閉合:** #69 的複驗條件仍無解(第三批採不到,前提已變,見 0815c §3)。

**UI 仍未實跑驗證。** 網站雖已上線,但上一次 playwright 實跑是時點推演那版;
之後四輪改動都不觸及 UI 路徑,所以沒重跑——但「沒觸及」是讀程式碼的判斷,不是實測。
要驗:`npx vite preview --port 4173` 再用 playwright 截圖。
