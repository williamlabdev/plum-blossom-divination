# 交接:六爻引擎(2026-08-15 稍晚,接續 `HANDOFF_六爻引擎_0815.md`)

## 1. 現況一句話

上一棒留的三條裁決結掉兩條(#69 由 william 裁定留著、移出分母維持不做),
backlog #3「未來時點推演」做完了本體並**實測否決了它接回計分**;
兩顆 commit 全推,工作區乾淨,**CI 仍紅、仍從未跑過測試**。

---

## 2. 已完成

### (1) #69 保留集失分 —— 帳已結,程式碼未動

william 裁決 **(a) 留著 `c836acc`,承認保留集 58.3%**。

`CLAUDE.md`「元神／忌神的有力無力」那節的紅字標題改成「帳已結」,並補上裁決段。
留下的理由寫得很明白,因為它跟數字相反:**數字其實偏向 revert**,留著是因為
「保留集是用來否決『我看著訓練集編出來的規則』的,不是用來裁決古法怎麼寫」——
這正是它與「忌神持世」的差別(那條是看著訓練集兩則案例加進來的,這一版不是)。

同時寫進複驗條件:**第三批語料採集後,若這類「用神無氣但古人斷吉」的案例仍成群失分,
就該回頭 revert,而不是繼續替它找理由。**

「把 5 則問時機型案例移出校準分母」**維持不做**。

### (2) `058d1b3` 重構:斷卦本體改吃 TimeFrame,並補 `chartAt`

- `analyzeLiuyao` 內部所有 `ct.month.branch`／`ct.day.branch`／`ct.xunKong` 改走 `frame`,
  本體改名 **`analyzeAt(chart, frame, qt, intent)`**;`analyzeLiuyao` 保留為吃 `ChartTime`
  的薄包裝,**所有既有呼叫端(UI、校準、測試)一字未動**。
- `najia.ts` 新增 **`chartAt(chart, frame)`**:整盤搬到另一個時間框架,換掉六爻的
  旺衰長生空破沖、五行旺衰表、以及神煞裡由月建推得的天喜與旺日、月破。
  日柱相關神煞刻意不動(理由同 `withMonthBranch`)。

**無行為改變是驗過的,不是宣稱的**:重構前後各錄 **64,613 筆**完整輸出
(64卦 × 6動爻 × 21類別 × 4組日月 × 2種 intent,加 101 則校準報告,含每段斷語文字,
鍵排序後序列化)逐字比對,**差異 0 筆**。快照腳本是臨時檔,驗完已刪(做法見
CLAUDE.md「有效的工作方法」)。

另新增常設測試:**`chartAt` + `analyzeAt` 與「用該月建重新起卦」整份報告逐字相同**——
上一棒那則等價測試只保證單爻,而斷語裡還有神煞與五行旺衰表。

### (3) `c4e499b` 未來時點推演本體(backlog #3)

`projectMonths(chart, frame, qt)` 把同一卦推到未來十二個月各斷一次,回傳每月的
分數、判語、用神旺衰,以及「轉機月」(當下未達偏吉、而未來首次轉吉的月份)。
介面上是一列十二格色階,問時機時預設展開、問吉凶時收合。

**🔑 接回計分被實測否決,這是本次最重要的產出。**
最直白的接法(當下未達偏吉、未來十二月內某月轉吉 → 改判吉)實測:

| | 第一批命中 |
|---|---|
| 現況 | 40/53 = **75.5%** |
| 套用該規則 | 34/53 = **64.2%** |

救回 5 則(#0、#5、#8、#14、#40,正是古人「待某月而成」那批),**弄錯 12 則**。
成因是觸發率高達 **32.1%(17/53)**:十二個地支輪一遍,用神幾乎總有得令之月,
「未來會有好月份」根本不是稀有事件。古人斷「遲而後成」另有前提(用神有根、
有日月動爻生扶),那些前提目前只存在於斷語文字裡,沒有結構化欄位。

已記進 `CLAUDE.md`「重要的負面結果」**第三節**,連同警告:任何「再收緊一點條件」
只要條件是看著那 17 則挑出來的,就是「忌神持世」那個坑的重演。

因此 **`analyzeAt` 從不呼叫 `projectMonths`**,校準數字結構上不可能改變。

### (4) 文件同步

`CLAUDE.md`(模組地圖、負面結果第三節、backlog #3 劃掉、目前狀態、裁決段)、
`README.md`(模組地圖、功能列加時點推演、測試數 113→116)、
`docs/backlog-evaluation.md`(第二節補 0815 實測、優先序更新)。

### 驗證證據

```
npx tsc -b && npx oxlint src && npm run build   # 全乾淨
npm test                                        # 116 項全過（原 113 + 新增 3）
```
UI 以 playwright 實跑 `vite preview`,問吉凶／問時機兩種 intent 各截圖確認:
十二格版面正常、色階可辨、當下有外框、收合展開行為正確。

**校準數字未變**:第一批 75.5%、全部 67.3%、保留集 58.3%(與 `1c23845` 相同)。

---

## 3. 未完成與地雷

🔴 **CI 仍紅,且仍從未跑過測試。** 最新 run `31889239884` 12 秒就掛,失敗點不變:
workflow 第 2 步 `actions/configure-pages` —— **GitHub Pages 從未在 Settings 啟用**
(0812 起就這樣)。`npm ci`／`npm test`／`npm run build` 排在它後面,**一次都沒執行過**。
**上面所有綠燈證據都是本地跑的**,網站仍 404。

⚠️ **時點推演目前對命中率的貢獻是 0**(它不參與計分)。它的價值在敘述與後續研究,
不要在沒有新語料的情況下試著「調一調就接回去」。

⚠️ **保留集已被消耗三四次**,#69 的裁決又是在知道保留集數字的情況下做的。
再做大幅調參前應重新採集第三批(採集方法見 CLAUDE.md「案例從哪裡來」)。

⚠️ 本次未動 `data/classic-cases.json`、未動任何權重常數、未動 `QUESTION_MAP`、
未動 `analyzeAt` 的任何一行計分邏輯(除了把 `JI_THRESHOLD` 提到模組層,值不變)。

⚠️ 上一棒的 `HANDOFF_六爻引擎_0815.md` 的 §3/§4/§5 **已過期**(那三條裁決結掉兩條)。
以本檔與 `CLAUDE.md` 為準。

---

## 4. 下一步

**(1) 修 CI —— william 手動,擋住所有後續驗證**

在瀏覽器開 `https://github.com/williamlabdev/plum-blossom-divination/settings/pages`
→ Source 選 **GitHub Actions**(GitHub 對新 repo 的限制,無法自動化)。然後:

```bash
cd /Users/william/dev/source/side-projects/destiny/plum-lossom-divination
gh run rerun 31889239884 && gh run watch
```

**(2) 採集第三批語料(下一個真正推進準確度的動作)**

現有兩批都已被消耗:第一批是訓練集、保留集看過三四次。第三批要用來驗兩件事:
① #69 那類「用神無氣但古人斷吉」是否成群失分(決定要不要 revert `c836acc`);
② 時點推演接回計分是否在乾淨語料上仍是負向。
採集來源與收錄標準見 `CLAUDE.md`「案例從哪裡來」。

**(3) 若要再嘗試把時點推演接回計分**——先做這個前置,不要直接調條件:

把「用神有根」(非真空、有日月動爻生扶)從斷語文字**結構化成 `LiuyaoReport` 的欄位**。
那是古法允許斷「遲而後成」的前提,也是接回計分唯一可辯護的入口。
實作位置:`interpret.ts` 的旬空段(真空判定)與原神段(生扶判定)已各自算出這個資訊,
只是沒有留成欄位。

**(4) backlog #4 多爻作用結算層** —— 必須先有 §5 的產品決策,否則不要開工。

---

## 5. 勿碰／等待

**等 william 裁決(剩一條):**

1. **要不要加六爻搖錢法輸入(backlog #4 的真正閘門)** —— 這是**產品範圍決策**,
   不是工程量問題。梅花易數起卦法結構上只產生一個動爻,要支援多爻同動就得新增
   搖錢法輸入;核心難點是貪生忘剋屬圖解算而非加總,現行線性累加的 `interpret.ts`
   沒有可掛載處。另現有 43 則多爻案例**不能兼作測試集**(未做用神對應,且與保留集同源)。
   **不拍板就不要動多爻結算層。**

**勿碰:**

- 不要把問時機型案例移出校準分母(已裁決不做)。
- 不要靠再看保留集決定任何規則的去留——要看就是驗收,而且要先有第三批。
- `data/zhouyi.ts` 是整理來的資料檔,不要手改。
